# PSE84 secure m33 - TinyUSB provides its own host stack, so keep Zephyr's USB
# host/device stacks and their controller drivers disabled to avoid claiming
# the usbhs (DWC2) controller.
CONFIG_USB_HOST_STACK=n
CONFIG_UHC_DRIVER=n
CONFIG_USB_DEVICE_STACK=n
CONFIG_UDC_DRIVER=n
